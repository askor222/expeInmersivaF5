<form method="post" action="{{ path('app_assets_links_delete', {'id': assets_link.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assets_link.id) }}">
    <button class="btn">Delete</button>
</form>
