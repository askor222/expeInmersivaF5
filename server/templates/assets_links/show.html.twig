{% extends 'base.html.twig' %}

{% block title %}AssetsLinks{% endblock %}

{% block body %}
    <h1>AssetsLinks</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assets_link.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ assets_link.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ assets_link.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_assets_links_index') }}">back to list</a>

    <a href="{{ path('app_assets_links_edit', {'id': assets_link.id}) }}">edit</a>

    {{ include('assets_links/_delete_form.html.twig') }}
{% endblock %}
